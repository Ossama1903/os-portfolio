interface Directories {
  [key: string]: {
    [key: string]: {};
  };
}

const directories: Directories = {
  home: {
    kekw: {
      lol: {},
    },
    lmfao: {},
  },
};

export default directories;
